{
  "name": "@codex-native/sdk",
  "version": "0.0.1",
  "description": "Native NAPI-based Codex SDK - complete standalone implementation.",
  "main": "dist/index.mjs",
  "module": "dist/index.mjs",
  "types": "dist/index.d.mts",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs"
    }
  },
  "files": [
    "dist",
    "README.md",
    "npm",
    "*.node"
  ],
  "scripts": {
    "clean": "rm -rf dist *.node npm target/release/*.node && cargo clean --release",
    "clean:all": "rm -rf dist *.node npm target node_modules && cargo clean",
    "build": "npm run build:rust && npm run build:napi && npm run build:ts && npm run verify:build",
    "build:rust": "cargo fmt && cargo clippy --release -- -D warnings",
    "build:napi": "napi build --platform --release",
    "build:napi:debug": "napi build --platform",
    "build:ts": "tsup",
    "verify:build": "node -e \"const fs=require('fs');const hasNode=fs.existsSync('codex_native.darwin-arm64.node')||fs.existsSync('codex_native.linux-x64-gnu.node')||fs.existsSync('codex_native.win32-x64-msvc.node');const hasDist=fs.existsSync('dist/index.mjs')&&fs.existsSync('dist/index.d.mts');if(!hasNode||!hasDist){console.error('Build incomplete: missing .node or dist files');process.exit(1)}console.log('✓ Build verification passed')\"",
    "create-npm-dirs": "napi create-npm-dirs",
    "artifacts": "napi artifacts",
    "prepublishOnly": "napi prepublish -t npm",
    "pretest": "npm run build:ts",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "test:watch": "NODE_OPTIONS=--experimental-vm-modules jest --watch",
    "coverage": "NODE_OPTIONS=--experimental-vm-modules jest --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "cargo fmt --check && cargo clippy --release -- -D warnings && npm run typecheck",
    "version": "napi version",
    "release:patch": "npm version patch && npm run release",
    "release:minor": "npm version minor && npm run release",
    "release:major": "npm version major && npm run release",
    "publish:platforms": "npm run create-npm-dirs && node scripts/publish-platform-packages.mjs",
    "release": "npm run build && npm run test && npm run publish:platforms && node scripts/publish-sdk.mjs",
    "release:dry": "npm run build && npm run test && npm publish --dry-run"
  },
  "napi": {
    "binaryName": "codex_native",
    "targets": [
      "aarch64-apple-darwin",
      "x86_64-apple-darwin",
      "aarch64-unknown-linux-gnu",
      "x86_64-unknown-linux-gnu",
      "aarch64-unknown-linux-musl",
      "x86_64-unknown-linux-musl",
      "aarch64-pc-windows-msvc",
      "x86_64-pc-windows-msvc"
    ]
  },
  "optionalDependencies": {
    "@codex-native/sdk-darwin-arm64": "0.0.1",
    "@codex-native/sdk-darwin-x64": "0.0.1",
    "@codex-native/sdk-linux-arm64-gnu": "0.0.1",
    "@codex-native/sdk-linux-x64-gnu": "0.0.1",
    "@codex-native/sdk-linux-arm64-musl": "0.0.1",
    "@codex-native/sdk-linux-x64-musl": "0.0.1",
    "@codex-native/sdk-win32-arm64-msvc": "0.0.1",
    "@codex-native/sdk-win32-x64-msvc": "0.0.1"
  },
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@napi-rs/cli": "^3.4.1",
    "@openai/agents": "^0.2.1",
    "@types/node": "^22.0.0",
    "jest": "^29.7.0",
    "tsup": "^8.5.0",
    "typescript": "^5.7.2",
    "zod": "^3.25.76"
  }
}